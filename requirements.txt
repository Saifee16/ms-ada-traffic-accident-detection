# ============================================================
# Traffic Surveillance System — Requirements
# Python 3.12 | CPU-first, GPU-optional
# ============================================================

# Core CV & Detection
ultralytics==8.3.40
opencv-python==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

# Deep Learning (CPU default; CUDA path: install torch+cuda separately)
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1

# OCR
easyocr==1.7.2
paddleocr==2.8.1; sys_platform != "win32"  # optional swap-in

# Tracking (ByteTrack is built into ultralytics; StrongSORT deps below)
lap
filterpy==1.4.5

# Alerts
requests==2.32.3
aiohttp==3.10.10

# Config & CLI
pyyaml==6.0.2
click==8.1.7
rich==13.9.4             # pretty CLI output

# Storage
pandas==2.2.3
sqlalchemy==2.0.36       # future SQLite/Postgres adaptor

# Async / Concurrency
asyncio-mqtt==0.16.2     # optional MQTT future extension

# Logging
structlog==24.4.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.14.0
pytest-cov==5.0.0

# Evaluation / Notebooks
matplotlib==3.9.2
seaborn==0.13.2
scikit-learn==1.5.2
jupyterlab==4.3.0

# Optical flow (bundled in opencv — no extra install)
# scipy used for stats
scipy==1.14.1
